# Native third-party sources

This directory is kept in the package so RawKit's build hook can watch it.

Run `dart run rawkit:download_library` to install the pinned LibRaw source into
the ignored `libraw/` subdirectory before using RawKit.
