# Native third-party sources

This directory is kept in the package so RawKit's build hook can watch it.

Run `dart run rawkit:prepare_library desktop` to install the pinned LibRaw
source into this ignored `libraw/` subdirectory before an offline native build.
